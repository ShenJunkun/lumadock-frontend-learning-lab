services:
  api:
    build:
      context: ./backend
    environment:
      CORS_ORIGINS: http://127.0.0.1:5173,http://localhost:5173
      LUMADOCK_DATABASE_URL: sqlite:////data/lumadock.db
    ports:
      - "8001:8001"
    volumes:
      - lumadock-data:/data

volumes:
  lumadock-data:
